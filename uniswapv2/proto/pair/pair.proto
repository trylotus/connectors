// Code generated by the tool (https://github.com/trylotus/abi-gen-proto). DO
// NOT EDIT. Source file: <from connector-bot>
syntax = "proto3";

package pair;

import "google/protobuf/timestamp.proto";
import "lotus/lotus.proto";

option go_package = "github.com/trylotus/connectors/uniswapv2/pair";

message Approval {
  option (lotus.composite).index = {
    unique: true
    fields: [
      "ts",
      "tx_hash",
      "index"
    ]
  };
  option (lotus.composite).index = {
    fields: [
      "tx_hash",
      "index"
    ]
  };

  google.protobuf.Timestamp ts = 1;
  uint64 block_number = 2 [(lotus.index) = {}];
  bytes tx_hash = 3 [(lotus.bytes) = {format: FORMAT_TYPE_HEX}];
  uint64 index = 4;
  bytes owner = 5 [(lotus.bytes) = {format: FORMAT_TYPE_HEX}];
  bytes spender = 6 [(lotus.bytes) = {format: FORMAT_TYPE_HEX}];
  string value = 7 [(lotus.numeric) = {}];
}

message Burn {
  option (lotus.composite).index = {
    unique: true
    fields: [
      "ts",
      "tx_hash",
      "index"
    ]
  };
  option (lotus.composite).index = {
    fields: [
      "tx_hash",
      "index"
    ]
  };

  google.protobuf.Timestamp ts = 1;
  uint64 block_number = 2 [(lotus.index) = {}];
  bytes tx_hash = 3 [(lotus.bytes) = {format: FORMAT_TYPE_HEX}];
  uint64 index = 4;
  bytes sender = 5 [(lotus.bytes) = {format: FORMAT_TYPE_HEX}];
  string amount0 = 6 [(lotus.numeric) = {}];
  string amount1 = 7 [(lotus.numeric) = {}];
  bytes to = 8 [(lotus.bytes) = {format: FORMAT_TYPE_HEX}];
}

message Mint {
  option (lotus.composite).index = {
    unique: true
    fields: [
      "ts",
      "tx_hash",
      "index"
    ]
  };
  option (lotus.composite).index = {
    fields: [
      "tx_hash",
      "index"
    ]
  };

  google.protobuf.Timestamp ts = 1;
  uint64 block_number = 2 [(lotus.index) = {}];
  bytes tx_hash = 3 [(lotus.bytes) = {format: FORMAT_TYPE_HEX}];
  uint64 index = 4;
  bytes sender = 5 [(lotus.bytes) = {format: FORMAT_TYPE_HEX}];
  string amount0 = 6 [(lotus.numeric) = {}];
  string amount1 = 7 [(lotus.numeric) = {}];
}

message Swap {
  option (lotus.composite).index = {
    unique: true
    fields: [
      "ts",
      "tx_hash",
      "index"
    ]
  };
  option (lotus.composite).index = {
    fields: [
      "tx_hash",
      "index"
    ]
  };

  google.protobuf.Timestamp ts = 1;
  uint64 block_number = 2 [(lotus.index) = {}];
  bytes tx_hash = 3 [(lotus.bytes) = {format: FORMAT_TYPE_HEX}];
  uint64 index = 4;
  bytes sender = 5 [(lotus.bytes) = {format: FORMAT_TYPE_HEX}];
  string amount0_in = 6 [(lotus.numeric) = {}];
  string amount1_in = 7 [(lotus.numeric) = {}];
  string amount0_out = 8 [(lotus.numeric) = {}];
  string amount1_out = 9 [(lotus.numeric) = {}];
  bytes to = 10 [(lotus.bytes) = {format: FORMAT_TYPE_HEX}];
}

message Sync {
  option (lotus.composite).index = {
    unique: true
    fields: [
      "ts",
      "tx_hash",
      "index"
    ]
  };
  option (lotus.composite).index = {
    fields: [
      "tx_hash",
      "index"
    ]
  };

  google.protobuf.Timestamp ts = 1;
  uint64 block_number = 2 [(lotus.index) = {}];
  bytes tx_hash = 3 [(lotus.bytes) = {format: FORMAT_TYPE_HEX}];
  uint64 index = 4;
  string reserve0 = 5 [(lotus.numeric) = {}];
  string reserve1 = 6 [(lotus.numeric) = {}];
}

message Transfer {
  option (lotus.composite).index = {
    unique: true
    fields: [
      "ts",
      "tx_hash",
      "index"
    ]
  };
  option (lotus.composite).index = {
    fields: [
      "tx_hash",
      "index"
    ]
  };

  google.protobuf.Timestamp ts = 1;
  uint64 block_number = 2 [(lotus.index) = {}];
  bytes tx_hash = 3 [(lotus.bytes) = {format: FORMAT_TYPE_HEX}];
  uint64 index = 4;
  bytes from = 5 [(lotus.bytes) = {format: FORMAT_TYPE_HEX}];
  bytes to = 6 [(lotus.bytes) = {format: FORMAT_TYPE_HEX}];
  string value = 7 [(lotus.numeric) = {}];
}
